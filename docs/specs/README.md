# Specs 文档说明（合并版）

`docs/specs/` 已按“实现所需最小规范”完成归并，当前主规范如下：

## 主规范（当前生效）

- `docs/specs/SPEC_CORE.md`  
  覆盖 CLI、状态模型、设计冻结范围。
- `docs/specs/SPEC_STATUS_JSON.md`  
  覆盖 `opskit status --json` 机器可读输出契约。
- `docs/specs/SPEC_OPS_SECURITY.md`  
  覆盖安装交付、运行边界、安全与脱敏规则。
- `docs/specs/SPEC_TEST_ACCEPTANCE.md`  
  覆盖最小验收测试路径、阶段验收点，以及 non-strict/strict 门禁策略。

## 历史文档（归档）

历史拆分文档已移动到 `docs/specs/archive/`，仅作追溯参考，不作为当前实现依据。
