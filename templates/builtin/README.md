# Builtin Templates

本目录包含内置标准模板（标准模板模式）：

- `default-manage.json` (`generic-manage-v1`)
- `single-service-deploy.json` (`single-service-deploy-v1`)

每个模板的交付说明见：

- `templates/builtin/default-manage.README.md`
- `templates/builtin/single-service-deploy.README.md`
